import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    static let quickAdd   = Self("quickAdd",   default: .init(.space, modifiers: .option))
    static let focusMode  = Self("focusMode",  default: .init(.f,     modifiers: .control))
}
